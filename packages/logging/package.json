{
  "name": "@primo-poker/logging",
  "version": "1.0.0",
  "description": "Structured logging for Cloudflare Workers",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf dist",
    "test": "jest",
    "test:ci": "jest --coverage",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@primo-poker/shared": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250731.0",
    "@types/jest": "^30.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.3"
  }
}